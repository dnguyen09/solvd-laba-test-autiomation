{
  "0": {
    "userId": "${userId}",
    "id": "1",
    <#if title??>
    <#if body??>
    "title": "${title}",
    "body": "${body}"
    <#else> "title": "${title}"
    </#if>,
    <#else> "body": "${body}"
    </#if>
  },
  "id": "regex:[\\d]"
}